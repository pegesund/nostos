# expect_error: String` does not implement trait `Num
# Test: calling function that requires Num trait with non-Num type

addNumbers[T: Num](a: T, b: T) = a + b

main() = addNumbers("hello", "world")  # String doesn't implement Num
