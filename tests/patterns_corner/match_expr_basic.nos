# expect: 42
# Basic match expression

main() = {
    x = 2
    result = match x {
        0 -> "zero"
        1 -> "one"
        2 -> "two"
        _ -> "other"
    }
    assert_eq("two", result)
    42
}
