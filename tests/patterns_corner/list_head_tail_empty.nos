# expect: 42
# Head/tail pattern with empty tail

headOnly([h | []]) = h
headOnly(_) = 0

main() = {
    assert_eq(5, headOnly([5]))
    assert_eq(0, headOnly([1, 2]))
    assert_eq(0, headOnly([]))
    42
}
