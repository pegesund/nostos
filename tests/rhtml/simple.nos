# expect: "test"

use stdlib.rhtml.{render}

main() = {
    result = RHtml(
        component("test", p("Hello"))
    )

    # Check we get an RHtmlResult with components
    match result.components {
        [] -> "no components"
        _ -> "test"
    }
}
