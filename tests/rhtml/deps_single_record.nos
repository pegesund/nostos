# expect: 1
# Deps tracking: single reactive record should have 1 entry

use stdlib.rhtml.*

reactive User = { name: String }

main() = {
    user = User("Alice")
    greeting = () => RHtml(h1(user.name))
    result = RHtml(div([component("greeting", greeting)]))
    Map.size(result.deps)
}
