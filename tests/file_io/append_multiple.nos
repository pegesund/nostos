# Multiple appends to same file
# expect: true

main() = {
  path = "/tmp/nostos_test_append_multi.txt"

  File.writeAll(path, "A")
  File.append(path, "B")
  File.append(path, "C")
  File.append(path, "D")

  data = File.readAll(path)
  File.remove(path)
  data == "ABCD"
}
