# Reading a nonexistent file should throw an exception
# expect: true

main() = {
  path = "/tmp/nostos_nonexistent_file_12345.txt"
  result = try {
    File.readAll(path)
    false
  } catch { e -> true }
  result
}
