# Copy file
# expect: true

main() = {
  src = "/tmp/nostos_test_copy_src.txt"
  dst = "/tmp/nostos_test_copy_dst.txt"
  content = "Copy me!"

  File.writeAll(src, content)
  File.copy(src, dst)
  data = File.readAll(dst)
  File.remove(src)
  File.remove(dst)
  data == content
}
