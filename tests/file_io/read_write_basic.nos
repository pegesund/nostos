# Basic file read/write test
# expect: true

main() = {
  path = "/tmp/nostos_test_rw_basic.txt"
  content = "Hello, Nostos!"

  File.writeAll(path, content)
  data = File.readAll(path)
  File.remove(path)
  data == content
}
