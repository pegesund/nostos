# File handle read with size
# expect: true

main() = {
  path = "/tmp/nostos_test_handle_read.txt"

  File.writeAll(path, "ABCDEFGHIJ")

  handle = File.open(path, "r")
  # Read 5 bytes
  data = File.read(handle, 5)
  File.close(handle)
  File.remove(path)
  data == "ABCDE"
}
