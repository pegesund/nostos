# Get file size
# expect: true

main() = {
  path = "/tmp/nostos_test_file_size.txt"
  content = "Hello World!"  # 12 bytes

  File.writeAll(path, content)
  size = File.size(path)
  File.remove(path)
  size == 12
}
