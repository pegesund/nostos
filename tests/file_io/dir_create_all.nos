# Create nested directory structure
# expect: true

main() = {
  path = "/tmp/nostos_test_dir_nested/a/b/c"

  Dir.createAll(path)
  exists = Dir.exists(path)
  Dir.removeAll("/tmp/nostos_test_dir_nested")
  exists
}
