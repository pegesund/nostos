# File handle flush
# expect: true

main() = {
  path = "/tmp/nostos_test_handle_flush.txt"

  handle = File.open(path, "w")
  File.write(handle, "Flushed")
  File.flush(handle)
  File.close(handle)
  File.remove(path)
  true
}
