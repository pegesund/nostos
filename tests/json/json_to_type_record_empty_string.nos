# expect: 0
# Test jsonToType with empty string
use stdlib.json.{jsonParse, jsonStringify}
type Label = { text: String }

main() = {
    json = jsonParse("{\"text\": \"\"}")
    lbl = jsonToType[Label](json)
    length(lbl.text)
}
