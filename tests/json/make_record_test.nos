# expect: true

type Person = { name: String, age: Int }

main() = {
    fields = %{
        "name": String("Alice"),
        "age": Integer(30)
    }

    person = makeRecord[Person](fields)

    person.name == "Alice" && person.age == 30
}
