# expect: 10
# Trait implemented on String type

trait Lengthy
    getLen(self) -> Int
end

String: Lengthy
    getLen(self) = length(self)
end

doubleLen[T: Lengthy](x: T) -> Int = x.getLen() * 2

main() = {
    doubleLen("hello")
}
