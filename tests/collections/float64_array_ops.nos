# expect: 0
# Test Float64Array operations - set and read back

main() = {
    arr = newFloat64Array(3)
    arr[0] = 3.14
    arr[1] = 2.71
    arr[2] = 1.41

    # Verify values
    assert(arr[0] > 3.13 && arr[0] < 3.15)
    assert(arr[1] > 2.70 && arr[1] < 2.72)
    assert(arr[2] > 1.40 && arr[2] < 1.42)
    assert(length(arr) == 3)
    0
}
