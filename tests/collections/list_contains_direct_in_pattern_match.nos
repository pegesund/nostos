# expect: true
# Regression test: .contains() called directly on a parameter in a list-pattern-matched function
# Previously failed with "cannot resolve trait method `contains` without type information"
check([], _) = false
check([_ | _], names) = names.contains("a")
main() = check([1], ["a", "b"])
