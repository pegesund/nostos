# expect_error: does not implement trait `Hash`
type NotHashable = NotHashable(Int)

hash_second[T, U: Hash](x: T, y: U) -> Int = hash(y)

main() = {
    nh = NotHashable(42)
    result = hash_second(42, nh)
    result
}
