# expect: "passed"
type Pair = Pair(Int, Int) deriving (Hash + Eq)

compare[T: Eq](x: T, y: T) -> Bool = x == y

main() = {
    p1 = Pair(1, 2)
    p2 = Pair(1, 2)
    p3 = Pair(3, 4)
    assert(compare(p1, p2) == true)
    assert(compare(p1, p3) == false)
    "passed"
}
