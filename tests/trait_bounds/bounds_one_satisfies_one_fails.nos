# expect_error: does not implement trait
type Good = Good(Int) deriving Hash
type Bad = Bad(Int)

need_two[T: Hash, U: Hash](x: T, y: U) -> Int = hash(x) + hash(y)

main() = {
    g = Good(1)
    b = Bad(2)
    result = need_two(g, b)
    result
}
