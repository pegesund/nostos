# expect: passed
type A = A(Int)
type B = B(String)

both_hashable[T: Hash, U: Hash](x: T, y: U) -> Int = hash(x) + hash(y)

main() = {
    result = both_hashable(A(1), B("hello"))
    assert(result != 0)
    "passed"
}
