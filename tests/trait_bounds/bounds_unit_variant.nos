# expect: passed
type Color = Red | Green | Blue

hashable[T: Hash](x: T) -> Int = hash(x)

main() = {
    r = Red
    g = Green
    hr = hashable(r)
    hg = hashable(g)
    assert(hr != hg)
    "passed"
}
