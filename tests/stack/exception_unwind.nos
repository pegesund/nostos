# expect: 0
# Exception unwinding through deep stack

level5() = throw("bottom")
level4() = level5()
level3() = level4()
level2() = level3()
level1() = level2()

main() = {
    result = try { level1() } catch { e -> e }
    assert_eq("bottom", result)
    0
}
