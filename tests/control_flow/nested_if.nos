# expect: ()
# Nested if expressions
# sign(5)=1, sign(-3)=-1, sign(0)=0 => 1 + (-1) + 0 = 0

sign(n) = if n > 0 then 1 else if n < 0 then -1 else 0

main() = {
    assert_eq(0, sign(5) + sign(-3) + sign(0)),
    ()
}
