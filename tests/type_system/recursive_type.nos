# expect: 10
# Recursive type (Tree)

type Tree[T] = Leaf(T) | Node(Tree[T], Tree[T])

sumTree(tree) = match tree
    Leaf(v) -> v
    Node(l, r) -> sumTree(l) + sumTree(r)
end

main() = {
    Tree[Int] t = Node(Node(Leaf(1), Leaf(2)), Node(Leaf(3), Leaf(4)))
    sumTree(t)
}
