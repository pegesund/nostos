# expect: 42
# Option variant - Some case

type Option[T] = Some(T) | None

unwrap(opt) = match opt {
    Some(x) -> x
    None -> 0
}

main() = {
    maybe: Option[Int] = Some(42)
    unwrap(maybe)
}
