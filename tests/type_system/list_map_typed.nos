# expect: [2, 4, 6]
# List map with typed result

map(f, xs) = match xs {
    [] -> []
    [h | t] -> [f(h) | map(f, t)]
}

main() = {
    nums: List[Int] = [1, 2, 3]
    doubled: List[Int] = map(x => x * 2, nums)
    doubled
}
