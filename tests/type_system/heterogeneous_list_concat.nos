# expect_error: type mismatch
# Type checker correctly constrains list element types through concat (++).
# f(xs) = ["hello"] ++ xs constrains xs to [String], so f([1,2,3]) is a type error.
f(xs) = ["hello"] ++ xs
main() = f([1, 2, 3])
