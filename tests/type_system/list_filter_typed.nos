# expect: [2, 4]
# List filter with typed result

filter(p, xs) = match xs
    [] -> []
    [h | t] -> if p(h) then [h | filter(p, t)] else filter(p, t)
end

main() = {
    nums: List[Int] = [1, 2, 3, 4, 5]
    evens: List[Int] = filter(x => x % 2 == 0, nums)
    evens
}
