# expect: 15
# List fold with typed accumulator

fold(f, acc, xs) = match xs
    [] -> acc
    [h | t] -> fold(f, f(acc, h), t)
end

main() = {
    nums: List[Int] = [1, 2, 3, 4, 5]
    sum: Int = fold((a, b) => a + b, 0, nums)
    sum
}
