# expect: medium
# Guards with typed result

classify(n) = match n {
    x when x > 100 -> "large"
    x when x > 10 -> "medium"
    _ -> "small"
}

main() = {
    result: String = classify(50)
    result
}
