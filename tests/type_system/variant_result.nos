# expect: 100
# Result variant type

type Result[T, E] = Ok(T) | Err(E)

unwrapOr(res, default) = match res
    Ok(x) -> x
    Err(_) -> default
end

main() = {
    success: Result[Int, String] = Ok(100)
    unwrapOr(success, 0)
}
