# expect: 15
# Either variant type

type Either[L, R] = Left(L) | Right(R)

getValue(e) = match e {
    Left(x) -> x
    Right(x) -> x
}

main() = {
    left: Either[Int, Int] = Left(5)
    right: Either[Int, Int] = Right(10)
    getValue(left) + getValue(right)
}
