# Test basic decorator functionality
# expect: 42

# Define a simple decorator template that doubles the result
# Note: fn decorator receives the full function, use ~fn.body to access body
template double(fn) = quote {
    result = ~fn.body
    result * 2
}

# Apply decorator to a function
@double
get_value() = 21

main() = get_value()
