# expect: 0
# Write computed value to mvar

mvar result: Int = 0

square(n) = n * n

set_squared(n) = {
    result = square(n)
    result
}

getValue() = result

main() = {
    assert_eq(0, getValue())
    assert_eq(25, set_squared(5))
    assert_eq(100, set_squared(10))
    0
}
