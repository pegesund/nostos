# expect: 0
# Reassign mvar to same value

mvar value: Int = 42

getValue() = value

set_same() = {
    value = value
    value
}

main() = {
    assert_eq(42, getValue())
    assert_eq(42, set_same())
    assert_eq(42, set_same())
    assert_eq(42, getValue())
    0
}
