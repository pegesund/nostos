# expect: 0
# Mvar initialized to zero

mvar counter: Int = 0

getValue() = counter

setCounter(n) = {
    counter = n
    counter
}

main() = {
    assert_eq(0, getValue())
    assert_eq(42, setCounter(42))
    assert_eq(42, getValue())
    0
}
