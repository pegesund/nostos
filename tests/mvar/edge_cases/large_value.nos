# expect: 0
# Mvar with large values

mvar big: Int = 1000000

getBig() = big

multiply(n) = {
    big = big * n
    big
}

main() = {
    assert_eq(1000000, getBig())
    assert_eq(10000000, multiply(10))
    assert_eq(100000000, multiply(10))
    0
}
