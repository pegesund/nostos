# expect: 0
# Send and receive a tuple message

worker(parent) = {
    parent <- (5, 10)
}

main() = {
    me = self()
    spawn { worker(me) }
    result = receive { (a, b) -> a + b }
    assert_eq(15, result)
    0
}
