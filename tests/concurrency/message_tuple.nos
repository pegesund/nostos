# expect: ()
# Send and receive a tuple message

worker(parent) = {
    parent <- (5, 10)
}

main() = {
    me = self()
    spawn { worker(me) }
    result = receive
        (a, b) -> a + b
    end
    assert_eq(15, result)
}
