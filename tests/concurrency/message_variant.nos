# expect: ()
# Send and receive a simple integer message

worker(parent) = {
    parent <- 100
}

main() = {
    me = self()
    spawn { worker(me) }
    result = receive
        n -> n
    end
    assert_eq(100, result)
}
