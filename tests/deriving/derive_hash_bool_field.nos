# expect: "passed"
type Flag = { enabled: Bool } deriving Hash

main() = {
    t1 = Flag(true)
    t2 = Flag(true)
    f = Flag(false)
    assert(hash(t1) == hash(t2))
    assert(hash(t1) != hash(f))
    "passed"
}
