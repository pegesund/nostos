# expect: "passed"
# Hash should be consistent - same value always produces same hash
type Data = Data(Int, String) deriving Hash

main() = {
    d = Data(42, "test")
    h1 = hash(d)
    h2 = hash(d)
    h3 = hash(d)
    assert(h1 == h2)
    assert(h2 == h3)
    "passed"
}
