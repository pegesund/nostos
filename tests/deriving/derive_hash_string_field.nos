# expect: "passed"
type Named = { name: String } deriving Hash

main() = {
    n1 = Named("hello")
    n2 = Named("hello")
    n3 = Named("world")
    n4 = Named("")
    assert(hash(n1) == hash(n2))
    assert(hash(n1) != hash(n3))
    assert(hash(n3) != hash(n4))
    "passed"
}
