# expect: "passed"
type Person = { name: String, age: Int, active: Bool } deriving Hash

main() = {
    p1 = Person("Alice", 30, true)
    p2 = Person("Alice", 30, true)
    p3 = Person("Bob", 30, true)
    assert(hash(p1) == hash(p2))
    assert(hash(p1) != hash(p3))
    "passed"
}
