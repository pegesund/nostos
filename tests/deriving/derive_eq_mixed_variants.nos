# expect: "passed"
type Result = Ok(Int) | Err(String) | Empty deriving Eq

main() = {
    ok1 = Ok(42)
    ok2 = Ok(42)
    ok3 = Ok(100)
    err = Err("fail")
    assert(ok1 == ok2)
    assert(ok1 != ok3)
    assert(ok1 != err)
    "passed"
}
