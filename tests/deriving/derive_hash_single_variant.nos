# expect: "passed"
type Wrapped = Value(Int) deriving Hash

main() = {
    v1 = Value(42)
    v2 = Value(42)
    v3 = Value(100)
    assert(hash(v1) == hash(v2))
    assert(hash(v1) != hash(v3))
    "passed"
}
