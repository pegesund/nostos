# expect: 0
# Exception thrown from called function

risky(n) =
    if n < 0 then throw("negative")
    else n * 2

main() = {
    assert_eq(20, risky(10))
    result = try risky(-5) catch e -> e end
    assert_eq("negative", result)
    0
}
