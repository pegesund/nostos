# expect: 0
# Catch and rethrow exception

main() = {
    result = try { try { throw("original") } catch { e -> throw("wrapped: " ++ e) } } catch { msg -> msg }
    assert_eq("wrapped: original", result)
    0
}
