# expect: 0
# Exception with record value

type Error = { code: Int, message: String }

main() = {
    result = try { throw(Error(404, "not found")) } catch { Error(code, msg) -> code }
    assert_eq(404, result)
    0
}
