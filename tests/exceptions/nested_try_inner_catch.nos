# expect: 0
# Nested try - inner catch handles exception

main() = {
    result = try { try { throw("inner") } catch { "inner" -> "caught inside" } } catch { e -> "outer: " ++ e }
    assert_eq("caught inside", result)
    0
}
