# expect: 10
# Nested record pattern matching

type Inner = { x: Int, y: Int }
type Outer = { inner: Inner, z: Int }

main() = {
    data = Outer(Inner(3, 4), 3)
    match data {
        Outer(Inner(x, y), z) -> x + y + z
    }
}
