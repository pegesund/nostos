# expect: found hello
# String literal patterns

greet(msg) =
    match msg
        "hello" -> "found hello"
        "world" -> "found world"
        "foo" -> "found foo"
        _ -> "unknown"
    end

main() = greet("hello")
