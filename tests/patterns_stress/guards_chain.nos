# expect: large
# Multiple guards in sequence

classify(n) =
    match n
        x when x < 0 -> "negative"
        x when x == 0 -> "zero"
        x when x < 10 -> "small"
        x when x < 100 -> "medium"
        _ -> "large"
    end

main() = classify(500)
