# expect: 30
# Regression test: .toFloat() on record field + .sum() in cross-module generic function
# Previously dispatched .toFloat() to String.toFloat in monomorphized variants
use types.Score
use stats.total
main() = total([Score(player: "A", points: 10), Score(player: "B", points: 20)])
