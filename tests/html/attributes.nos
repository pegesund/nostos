# expect: <a href="https://example.com">Link</a>
# HTML attributes work correctly with named parameters

use stdlib.html.{Html, render}

main() = {
    # a(str, href: ...) uses named parameter syntax
    page = Html(a("Link", href: "https://example.com"))
    render(page)
}
