# expect: <p>&lt;script&gt;</p>
# HTML escaping works correctly

use stdlib.html.{Html, render}

main() = {
    # p("<script>") uses the overloaded p(str: String) variant
    page = Html(p("<script>"))
    render(page)
}
