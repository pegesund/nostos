# expect: <div><h1>Hello</h1></div>
# Basic HTML rendering with Html(...) scoped names

use stdlib.html.{Html, render}

main() = {
    # h1("Hello") uses the overloaded h1(str: String) variant
    page = Html(div([h1("Hello")]))
    render(page)
}
