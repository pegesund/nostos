# expect: <ul><li>A</li><li>B</li></ul>
# Nested HTML elements work correctly

use stdlib.html.{Html, render}

main() = {
    # li("A") uses the overloaded li(str: String) variant
    page = Html(ul([li("A"), li("B")]))
    render(page)
}
