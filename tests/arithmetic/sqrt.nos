# expect: 0
# Square root function

main() = {
    # Basic sqrt
    assert_eq(sqrt(4.0), 2.0)
    assert_eq(sqrt(9.0), 3.0)
    assert_eq(sqrt(16.0), 4.0)
    assert_eq(sqrt(1.0), 1.0)
    assert_eq(sqrt(0.0), 0.0)

    # Non-perfect squares (approximate)
    root2 = sqrt(2.0)
    assert(root2 > 1.41 && root2 < 1.42)
    0
}
