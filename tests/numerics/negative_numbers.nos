# expect: ()
# Negative number operations

main() = {
    assert_eq(-10, -5 + -5)
    assert_eq(0, -5 + 5)
    assert_eq(-25, -5 * 5)
    assert_eq(25, -5 * -5)
    assert_eq(-2, -10 / 5)
    assert_eq(2, -10 / -5)
}
