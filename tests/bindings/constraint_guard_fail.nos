# expect_error: Non-exhaustive match
# Constraint passes but guard fails - no match

main() = {
    x = 5
    match (5, 3) {
        (x, y) when y > 5 -> x + y   # x constraint passes, guard fails
    }
}
