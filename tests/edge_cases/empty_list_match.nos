# expect: "empty"
# Matching on empty list

handle_list(list) =
    match list
        [] -> "empty"
        [x] -> "one"
        [x, y] -> "two"
        _ -> "many"
    end

main() = handle_list([])
