# expect: ()
# Ackermann function - tests deep recursion

ack(0, n) = n + 1
ack(m, 0) = ack(m - 1, 1)
ack(m, n) = ack(m - 1, ack(m, n - 1))

# ack(3, 2) = 29
main() = {
    assert_eq(29, ack(3, 2))
}
