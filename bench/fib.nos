# Fibonacci benchmark
fib(n) = if n <= 1 then n else fib(n - 1) + fib(n - 2)

main() = {
    start = Time.now()
    result = fib(40)
    elapsed = Time.now() - start
    println("TIME:" ++ show(elapsed))
    println("RESULT:" ++ show(result))
    result
}
