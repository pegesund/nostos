# Test: Check if element in list
elem(_, []) = false
elem(x, [y | ys]) = if x == y then true else elem(x, ys)

b2i(b) = if b then 1 else 0

main() = {
    lst = [3, 1, 4, 1, 5, 9]
    b2i(elem(4, lst)) + b2i(!elem(7, lst)) * 10 + b2i(elem(9, lst)) * 100
}
