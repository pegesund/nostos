# Benchmark: Closure calling
# Tests closure invocation overhead

add_100(x) = x + 100

call_many_times(0, acc) = acc
call_many_times(n, acc) = {
    adder = \x => x + 100
    result = adder(n)
    call_many_times(n - 1, acc + result)
}

main() = call_many_times(3000, 0)
