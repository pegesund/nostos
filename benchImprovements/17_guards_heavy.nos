# Benchmark: Guard conditions
# Tests guard evaluation overhead

classify(n) =
    if n < 0 then -1
    else if n == 0 then 0
    else if n < 10 then 1
    else if n < 100 then 2
    else if n < 1000 then 3
    else 4

sum_classes(0, acc) = acc
sum_classes(n, acc) = {
    c = classify(n)
    sum_classes(n - 1, acc + c)
}

main() = sum_classes(5000, 0)
